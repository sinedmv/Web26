<!DOCTYPE html>
<html lang="ru">
{{> head
        description="Редактирование пользователя"
        keywords="пользователь, редактировать"
        title="Редактирование пользователя"
}}
<body class="body">
{{> header pageTitle="Редактирование пользователя"}}

<main class="main">
    <section class="user-form">
        <h2 class="user-form__title">Редактирование пользователя</h2>

        <form action="/users/{{user.id}}?_method=PATCH" method="POST" class="form">
            <div class="form__group">
                <label for="username" class="form__label">Логин:</label>
                <input type="text" id="username" name="username"
                       value="{{user.username}}" class="form__input" required>
            </div>

            <div class="form__group">
                <label for="email" class="form__label">Email:</label>
                <input type="email" id="email" name="email"
                       value="{{user.email}}" class="form__input" required>
            </div>

            <div class="form__group">
                <label for="password" class="form__label">Новый пароль (оставьте пустым, чтобы не менять):</label>
                <input type="password" id="password" name="password" class="form__input">
            </div>

            {{#if isAuthorized}}
                <div class="form__group">
                    <label for="isAdmin" class="form__label">
                        <input type="checkbox" id="isAdmin" name="isAdmin"
                               {{#if user.isAdmin}}checked{{/if}}>
                        Администратор
                    </label>
                </div>
            {{/if}}

            <div class="form__actions">
                <button type="submit" class="btn btn-primary">Сохранить</button>
                <a href="/users/{{user.id}}" class="btn">Отмена</a>
            </div>
        </form>
    </section>
</main>

{{> footer}}
</body>
</html>